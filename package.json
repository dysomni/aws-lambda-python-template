{
  "name": "aws-lambda-ruby-template",
  "version": "1.0.0",
  "scripts": {
    "bundle": "bundle install",
    "build": "./config/build.sh",
    "push": "./config/deploy.sh",
    "deploy": "source .env && yarn bundle && yarn build && yarn push"
  },
  "author": "",
  "license": "ISC"
}
